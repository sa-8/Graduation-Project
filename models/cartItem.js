class CartItem{
    constructor(imageUrl,quantity,productTitle,productPrice,total){
       this.imageUrl=imageUrl;
       this.quantity=quantity;
       this.productTitle=productTitle;
       this.productPrice=productPrice;
       this.total=total
    }
}
export default CartItem